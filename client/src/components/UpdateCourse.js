import React, { Component } from 'react';
import { Link } from 'react-router-dom';

export default class UpdateCourse extends Component {
  render() {
    return (
      <div>
        <h1>Update Course Component!</h1>
      </div>
    );
  }
}
